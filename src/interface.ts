
export interface IRankingData {
    index: string;
    imgUrl: string;
    rankTitle: string;
    rankSubtitle: string;
    scale: string;
    series: string;
}

export interface IRankArticle {
    icon: string;
    title: string;
    subtitle: string;
}

export interface IImageData {
    image: string;
}

